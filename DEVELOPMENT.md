# Development

This repository is currently a lightweight agent-oriented scaffold.

This file is for humans setting up and working in the local development environment.

## Local Setup

- Install the project-specific tooling that the repo eventually defines.
- If a package manager manifest is added later, use the lockfile-preferred install command.
- Review `AGENTS.md` and `.docs/conventions/README.md` before making changes.

## What To Document Here

Add the exact setup steps when the project gains a runtime:

- Required language/runtime versions
- Dependency installation steps
- Environment variables and secrets
- Start, test, lint, and format commands
- Any platform-specific setup notes

## Keep This Current

When a new developer or agent workflow is introduced, capture it here instead of burying it in chat history or commit messages.
