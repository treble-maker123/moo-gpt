{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; echo \"$f\" | grep -q 'src/agent/graph/' && npm run update:graph; } 2>/dev/null || true",
            "statusMessage": "Updating graph diagram..."
          }
        ]
      }
    ]
  }
}
